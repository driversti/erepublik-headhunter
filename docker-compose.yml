services:
  bot:
    build: .
    image: headhunter:local
    container_name: headhunter
    restart: unless-stopped
    env_file: .env
    environment:
      # All other vars are pulled from .env via env_file. The ones listed here
      # have to be templated so DATABASE_URL composes cleanly across hosts.
      DATABASE_URL: postgres://headhunter:${DB_PASSWORD:-headhunter}@db:5432/headhunter
      HTTP_PORT: 3000
    ports:
      - "${HTTP_PORT:-3000}:3000"
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    container_name: headhunter-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: headhunter
      POSTGRES_PASSWORD: ${DB_PASSWORD:-headhunter}
      POSTGRES_DB: headhunter
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U headhunter -d headhunter"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db_data:
